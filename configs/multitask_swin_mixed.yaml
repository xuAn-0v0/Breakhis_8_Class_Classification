# Swin Tiny 多任务学习 - Mixed 数据集
experiment_name: "multitask_swin_mixed"
group: "swin"

# Model Configuration
model:
  name: "timm_multitask"
  backbone: "swin_tiny_patch4_window7_224"
  num_classes_binary: 2
  num_classes_multiclass: 8
  use_multiclass: true
  dropout: 0.5
  pretrained: true

# Data Configuration
data:
  dataset_root: "/songjian/zixuan/dataset_cancer_v1"
  train_csv: "train_split_40x_100x_mixed.csv"
  val_csv: "val_split_40x_100x_mixed.csv"
  test_csv_40x: "test_split_40x.csv"
  test_csv_100x: "test_split_100x.csv"
  image_size: 224
  batch_size: 32
  num_workers: 4

# Training Configuration
training:
  epochs: 100
  optimizer: "adam"
  learning_rate: 0.0001
  weight_decay: 0.0001
  scheduler: "cosine"
  loss: "focal_loss"
  focal_alpha: 1.0
  focal_gamma: 2.0
  alpha_multiclass: 1.0
  early_stopping: true
  patience: 15

# Checkpoint Configuration
checkpoint:
  save_dir: "multitask_results/checkpoints/swin_mixed"
  save_best_only: true
  save_freq: 10

# Results Configuration
results:
  save_dir: "multitask_results/results/swin_mixed"
  save_predictions: true
  plot_confusion_matrix: true
  plot_training_history: true

# Device Configuration
device: "cuda"
seed: 42


